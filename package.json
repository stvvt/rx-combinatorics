{
  "name": "rx-combinatorics",
  "version": "2.2.3",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha --require ts-node/register --watch-extensions ts \"test/**/*.spec.ts\"",
    "test:watch": "mocha --require ts-node/register --watch --watch-extensions ts \"test/**/*.spec.ts\"",
    "build": "tsc -p tsconfig.build.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.0.5",
    "@types/mocha": "^2.2.44",
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "ts-node": "^3.3.0",
    "tslint": "^5.8.0",
    "typescript": "^2.6.1"
  },
  "dependencies": {
    "fast-memoize": "^2.2.8"
  }
}
